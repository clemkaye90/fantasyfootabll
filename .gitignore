# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Local secrets (API keys) - never commit. See .streamlit/secrets.toml.example
.streamlit/secrets.toml

# Regenerated from corpus/ via scripts/export_txt_for_lyzr.py -- same
# content, just .txt instead of .md for Lyzr's uploader. Not worth
# duplicating in git history.
corpus_txt/

# Live, locally-growing state written by the scheduled
# scripts/capture_spread_snapshot.py task -- unlike the other bundled
# .db files, this isn't a curated snapshot meant to ship with the app.
src/nfl_fantasy_app/data/spread_snapshots.db
logs/
